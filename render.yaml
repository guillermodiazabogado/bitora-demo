services:
  - type: web
    name: bitora-demo
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/app.py
    healthCheckPath: /health
    envVars:
      - key: APP_ENV
        value: demo
      - key: BASE_URL
        value: https://bitora-demo.onrender.com
      - key: HTTPS_REQUIRED
        value: "true"
      - key: QR_DB_ENGINE
        value: sqlite
      - key: QR_SQLITE_PATH
        value: acreditaciones.sqlite3
      - key: QR_POSTGRES_DSN
        sync: false
      - key: QR_POSTGRES_POOL_MIN
        value: "1"
      - key: QR_POSTGRES_POOL_MAX
        value: "10"
      - key: QR_AUTO_BACKUP_MINUTES
        value: "10"
      - key: QR_BACKUP_KEEP_LAST
        value: "24"
