# V4.8 Observability

Existing runtime metrics cover route latency and status. Operational writes
are audited with event and organization scope. Error responses are sanitized;
alert generation is deduplicated by stable key.

