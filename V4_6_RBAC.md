# BITORA V4.6 - RBAC

Permisos: `zones.read`, `zones.manage`, `zones.assign`, `zones.validate`, `zones.override`, `zones.access_log.read`, `zones.audit.read`.

Operador de acceso solo valida. Overrides quedan separados.
