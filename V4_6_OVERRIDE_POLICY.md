# BITORA V4.6 - Override Policy

Los overrides requieren permiso `zones.override`, motivo obligatorio, actor, zona, persona/acreditacion y auditoria.

`DENY_OVERRIDE` y `ALLOW_OVERRIDE` tienen precedencia sobre la asignacion ordinaria.
