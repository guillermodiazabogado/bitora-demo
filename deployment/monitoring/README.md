# BDF Monitoring

El monitoreo basico de staging lo ejecuta:

```text
deployment/scripts/bdf_monitor.py
```

Los logs reales se escriben en el volumen `/bitora/logs` y en `deployment/logs/` para comandos BDF locales.
