# BDF Config

Reservado para configuraciones no secretas de despliegue.

Los secretos y variables reales deben vivir en:

```text
deployment/staging/.env.staging
```

Ese archivo esta ignorado por Git.
