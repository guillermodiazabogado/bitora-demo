# V4.8 Actions Contract

Incident/task creation and status changes are backend-authorized, event-scoped
and audited. The first release deliberately avoids duplicating domain actions;
future quick actions must delegate to the owning service.

