# Google Integration Setup

Estado: preparado a nivel de arquitectura.

Variables previstas:

- `GOOGLE_OAUTH_CLIENT_ID`
- `GOOGLE_OAUTH_CLIENT_SECRET`
- `GOOGLE_OAUTH_REDIRECT_URI`

Pendiente:

- crear app OAuth en Google Cloud;
- configurar redirect URI;
- implementar callback final;
- guardar refresh token cifrado por organizacion;
- probar importacion/sincronizacion por organizacion.
