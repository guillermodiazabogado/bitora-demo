# V4.3 Template Versioning

Estados:

- `DRAFT`
- `PUBLISHED`
- `RETIRED`

Una emision solo puede usar versiones `PUBLISHED`. El contenido publicado conserva `content_hash`, `renderer_version`, `published_at` y `published_by`.
