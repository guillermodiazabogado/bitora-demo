# V4.8 Readiness Model

Readiness is explainable rather than opaque: each check contains code,
category, status, severity, message, source, recommendation and timestamp.
`BLOCKED`, `WARNING`, `READY`, `PENDING` and `NOT_APPLICABLE` are supported.

