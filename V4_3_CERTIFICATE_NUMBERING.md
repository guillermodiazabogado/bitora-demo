# V4.3 Certificate Numbering

Formato:

`BITORA-{organization_id}-{event_id}-{certificate_type_code}-{sequence}`

La secuencia se guarda en `certificate_number_sequences`. La unicidad se aplica por organizacion, evento y numero para permitir restores convivientes sin alterar documentos historicos.
